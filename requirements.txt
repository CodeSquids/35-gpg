# IMAP Server Requirements
# This project aims to use only the Python standard library.
# No external dependencies are required.

# However, if you wish to run tests, you'll need pytest:
# pytest>=6.0.0

# For type checking during development, you might want:
# mypy>=0.9.0

# But for production/runtime, only Python 3.11+ is required.